# Engram memory (DB, WAL, SHM)
.engram/
*.db
*.db-shm
*.db-wal

# Node
node_modules/
.npm/

# TypeScript builds
dist/
*.tsbuildinfo
.tsbuild/

# pnpm lockfiles (not used in this repo; v2.0 removed them)
pnpm-lock.yaml
pnpm-workspace.yaml

# Bun lockfile variants (we use bun test, not bun install as a workflow)
bun.lockb

# Logs and backups
*.log
*.bak
*.bak.*
*.tmp
coverage/

# OS
.DS_Store
Thumbs.db

# Editors (commit team-shared .vscode/settings.json; ignore user-local overrides)
.vscode/
!.vscode/settings.json
.idea/
*.swp
*.swo
*~

# OpenCode local overrides
opencode.local.json
opencode.local.jsonc
